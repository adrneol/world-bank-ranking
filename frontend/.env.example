# Base URL of the ranking backend (no trailing slash).
# When unset, same-origin is used, which works with `vite dev` via the
# /api proxy and with any deployment that serves the API on the same host.
VITE_API_BASE_URL=
